# --- Secrets: never commit real credentials ---
app/config.php
.deploy.env

# --- User uploads: keep the folders, ignore their contents ---
uploads/*/*
!uploads/*/.gitkeep
!uploads/.htaccess

# --- OS / editor noise ---
.DS_Store
Thumbs.db
*.log
