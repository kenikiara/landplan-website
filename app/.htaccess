# Never serve the PHP core directly — it is only ever include()d.
# Works on both Apache 2.2 and 2.4.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
